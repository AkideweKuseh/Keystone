{
  "name": "@sam/worker",
  "version": "0.0.0",
  "private": true,
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "dev": "ts-node -r tsconfig-paths/register src/main.ts",
    "test": "vitest run"
  },
  "dependencies": {
    "@nestjs/common": "^10.0.0",
    "@nestjs/core": "^10.0.0",
    "@sam/auth": "workspace:*",
    "@sam/domain": "workspace:*",
    "@sam/drivers-core": "workspace:*",
    "@sam/drivers-hikvision": "workspace:*",
    "@sam/drivers-mock": "workspace:*",
    "@sam/persistence": "workspace:*",
    "@sam/queue": "workspace:*",
    "bullmq": "^5.76.9",
    "fast-xml-parser": "^5.8.0",
    "ioredis": "^5.10.1",
    "reflect-metadata": "^0.2.2"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "ts-node": "^10.9.0",
    "tsconfig-paths": "^4.2.0",
    "vitest": "^1.6.0"
  }
}
